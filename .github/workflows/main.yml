name: Deploy master
on:
  push:
    branches:
    - master    
jobs:
  build:    
    runs-on: ubuntu-latest
    steps:
      
      - name: Generate deployment package
        run: ${{secrets.AWS_ACCESS_KEY_ID}}
