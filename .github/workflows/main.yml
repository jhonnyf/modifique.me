name: Deploy master
on:
  push:
    branches:
    - master    
jobs:
  build:    
    runs-on: ubuntu-latest
    environment: production
    steps:      
      - name: Generate deployment package
        run: ${{ secrets.AWS_ACCESS_KEY_ID }} - ${{ env.AWS_ACCESS_KEY_ID }}
