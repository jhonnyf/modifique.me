name: Deploy master
on:
  push:
    branches:
    - master    
jobs:
  build:    
    runs-on: ubuntu-latest
    environment: production
    steps:      
      - name: Generate deployment package
        run: $GITHUB_RUN_ID
