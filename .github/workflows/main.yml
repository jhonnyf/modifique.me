name: Deploy master
on:
  push:
    branches:
    - master    
jobs:
  build:    
    runs-on: ubuntu-latest
    environment: production
    steps:      
      - name: Generate deployment package
        run: echo "Deploying to production server on branch ${{ secrets.AWS_DEFAULT_REGION }} - ${{ secrets.AWS_ACCESS_KEY_ID }}"
